# Result

## WorkerRef
`ID`: cache's immutable ref's id || worker id
`Release`: release immutable ref cache
`GetRemotes`: if worker instance has `GetRemotes` then worker's GetRemotes || Immutable Ref's GetRemote function